#pragma once

// Copie ce fichier en "wifi_secrets.h" (ignore par git, voir .gitignore) et
// renseigne tes propres identifiants avant de flasher. Ne jamais commit de
// vrai mot de passe.
#define WIFI_SSID "leafcan"
#define WIFI_PASS "changeme123"
